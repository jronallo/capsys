<h1>Listing sitemaps</h1>

<table>
  <tr>
    <th>Url</th>
    <th>Valid</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @sitemaps.each do |sitemap| %>
  <tr>
    <td><%= sitemap.url %></td>
    <td><%= sitemap.valid %></td>
    <td><%= link_to 'Show', sitemap %></td>
    <td><%= link_to 'Edit', edit_sitemap_path(sitemap) %></td>
    <td><%= link_to 'Destroy', sitemap, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Sitemap', new_sitemap_path %>
