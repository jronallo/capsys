<p id="notice"><%= notice %></p>

<h2><%= link_to @profile.name, @profile %></h2>

<p>
  <b>Sitemap URL:</b>
  <%= link_to @sitemap.url, @sitemap.url %>
</p>

<p>
  <b>Valid:</b>
  <%= @sitemap.valid_sitemap %>
</p>

<%= link_to 'Edit', edit_profile_sitemap_path(@profile) %> |
<%= link_to 'Back to Profile', profile_path(@profile) %>
<%= button_to "Destroy", {:action => :destroy, :id => @sitemap.id}, 
            :class => 'destroy', :method => :delete %>
